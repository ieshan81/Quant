# Railway / production install (lean). Installed by nixpacks.toml:
#   pip install --no-cache-dir -r requirements-deploy.txt
#
# Heavy ML (torch, transformers, huggingface_hub) intentionally omitted.
# FinBERT/social sentiment degrades to 0.0 when ML stack is unavailable.
# Full stack for local dev/CI: requirements.txt (+ optional requirements-ml.txt).

python-dotenv>=1.0.0
loguru>=0.7.0
pytz>=2024.1
psutil>=5.9.0

Flask>=3.0.0
flask-socketio>=5.3.0
eventlet>=0.35.0

alpaca-trade-api>=3.0.0
ccxt>=4.0.0

numpy>=1.24.0
pandas>=2.0.0
lxml>=5.0.0
yfinance>=0.2.28

praw>=7.7.0
feedparser>=6.0.0

websocket-client>=1.6.0
schedule>=1.2.0
APScheduler>=3.10.0
requests>=2.31.0
