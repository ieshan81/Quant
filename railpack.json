{
  "$schema": "https://schema.railpack.com",
  "packages": {
    "python": "3.11"
  },
  "steps": {
    "install": {
      "commands": [
        "cd /app && python -m pip install --upgrade pip",
        "cd /app && pip install --no-cache-dir -r /app/requirements-deploy.txt"
      ]
    }
  },
  "deploy": {
    "startCommand": "bash start.sh"
  }
}
