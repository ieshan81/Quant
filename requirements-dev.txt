# Local development + pytest (includes lean deploy set + tests + optional ML).
-r requirements-deploy.txt

pytest>=7.4.0

# Code knowledge graph (AST + report; optional for CI)
graphifyy>=0.8.15

# Optional HuggingFace sentiment (not installed on Railway deploy)
-r requirements-ml.txt
