# Railway Nixpacks — installs requirements.txt (includes requirements-deploy.txt).

providers = ["python"]

[variables]
NIXPACKS_PYTHON_VERSION = "3.11"
PIP_NO_CACHE_DIR = "1"
PIP_DISABLE_PIP_VERSION_CHECK = "1"

[phases.setup]
nixPkgs = ["python311", "gcc"]

[start]
cmd = "bash start.sh"
